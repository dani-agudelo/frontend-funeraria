import { NotificationInvoice } from './notification-invoice.model';

describe('NotificationInvoice', () => {
  it('should create an instance', () => {
    expect(new NotificationInvoice()).toBeTruthy();
  });
});
